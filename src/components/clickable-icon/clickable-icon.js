import React from 'react';
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';

const ClickableIcon = props => <FontAwesomeIcon {...props} cursor="pointer" />;

export default ClickableIcon;
